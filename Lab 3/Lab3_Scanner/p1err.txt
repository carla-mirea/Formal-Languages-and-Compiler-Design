be a number
be b number
be c number
be max integer

readFromConsole(a)
readFromConsole(b)
readFromConsole(c)

checkk (max > a) max = a
check (max > b) max = b
check (max > c) max = c

showConsole(max)